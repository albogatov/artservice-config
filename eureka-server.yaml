eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
    service-url:
      defaultZone:
        - "http://localhost:8762/eureka"

server:
  port: 8762
management:
  endpoints:
    web:
      exposure:
        include=*:
  security:
    enabled=false:
  endpoint:
    restart:
      enabled=true:
    env:
      post:
        enabled=true:
endpoints:
  actuator:
    enabled=true:
  sensitive=true:
